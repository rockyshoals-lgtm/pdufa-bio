# Builder ack — 09:00 slot — 2026-09-07
*Run started 09:07 PT (12:07 ET), ack written 09:24 PT. Facts and build mechanics only — not investment advice.*

The 08:20 slot did not fire (no process, no file, no partial commit beyond an uncommitted `_lib.mjs` edit that I adopted). This ack covers the 08:40 note's ORDER, which is the 08:00 ORDER carried.

## Live build
built: **2026-09-07T16:18:48+00:00** (12:18 ET) · build-info `commit`: **c3eec4e82** · commits: c3eec4e82 (the work), 59ba8f997 (build-info stamp naming c3eec4e82), 364aa861b (CI pillow fix, see below) · guards: **71 green** locally at 09:18 PT (68 existing + 3 new), zero conflict markers before commit · live confirmed at 09:20 PT: `/build-info.json` commit c3eec4e82, and every check below was run against https://www.pdufa.bio with `Cache-Control: no-cache` after that.

## ORDER items
| # | item | status | evidence (the auditor's acceptance check, run by me, live) |
|---|------|--------|---------------------------------------------------------------|
| 1 | C1 API cache policy | **DONE** | `curl -sI /api/v1/events?limit=500` → `cache-control: s-maxage=300, stale-while-revalidate=300, stale-if-error=3600` (was 1800/86400/604800; same string on `CDN-Cache-Control` and `Vercel-CDN-Cache-Control`). First GET after deploy: `x-vercel-cache: MISS`, age 0, `as_of` 2026-09-07, `total` 456; second GET 30 s later: HIT, age 30, identical `as_of`/`total`. `built` moved 03:47:52Z → 16:18:48Z. Which layer served the 87/49/38 calendar: **not determined** — Vercel's edge cache is keyed per deployment, so a 09-05 payload on the 03:47Z deployment should not have been servable; the observation is consistent with a region that had not yet received the new deployment at 15:01Z, but I cannot prove that from headers I do not have. What I can say: the ceiling is now 10 minutes (300 fresh + 300 stale) on any layer that honours the header. `/api/data` (the legacy noindexed app feed, `s-maxage=16000`) is unchanged and out of C1's scope; flag it if you want it lowered. |
| 2 | C2 stampers | **DONE** | `/calendar`: "September 7, 2026" ×11, no other date; `/fda-this-month` ×1; `/drug/camizestrant` ×1; `/runup-by-year`: "Sep 7, 2026" + "September 7, 2026" + "Last computed 2026-09-07" — one date per page, all = Eastern date of `built` (16:18Z = Sept 7 ET). Root cause was NOT the header stamper's own clock: `build_freshness_stamp.py` prints the content-change date from `_sitemap_lastmod.json`, and `build_sitemap.py` line 53 computed that date in **UTC** (`dt.datetime.now(dt.timezone.utc).date()`, with a comment defending UTC). The 03:47Z build therefore recorded 909 pages as changed on "2026-09-07" while the lede said September 6. Fixed: `build_sitemap.py` TODAY = `site_dates.eastern_today()`, and the paired `ts` is written in Eastern with its offset (`2026-09-07T12:17:08-04:00`) so `ts[:10] == date` and the file's own consistency repair never drops it; `build_runup_by_year.py` TODAY → `site_dates.eastern_today()` (that page also printed "Sep 7" from the machine's local clock, which agrees with UTC/Eastern by accident today). `build_drug_pages.py` was already on `site_dates`; the "September 7" on `/drug/camizestrant` was the header stamp, same root cause. Guard: `tests/test_one_date_per_page.py`, below. Planted-03:00Z-clock rendering was not built (no injectable clock in the pipeline yet); the guard asserts the render instead. |
| 3 | C3 NRXP | **DONE** | `/pdufa/NRXP`: "no public decision" ×0; "July 30, 2026" ×5; "major deficiency" ×5 (banner, story sentence, FAQ answer, FAQ JSON-LD, meta description). Text now: "on July 30, 2026 the FDA completed its first-cycle review, identifying no major deficiencies relating to the drug components and one major deficiency relating to the container closure system (the vial's Luer-lock tip); NRx says it has submitted the requested manufacturer's attestation and targets approval in 2026." Banner headline "First-cycle review completed", closing "No approval has been published as of September 7, 2026." Sources linked: NRx Q2 2026 Form 10-Q (EDGAR filing index for CIK 0001719406) and the NRx first-cycle release. Mechanism: `_goal_date_passed.json` gained `first_cycle_date` / `first_cycle_text` / `source_2`; `mark_goal_date_passed.py` renders the first-cycle action when present and retires its own earlier "no public decision" wording on re-render. Your 09-06c note asked for "container closure" and "no major deficiencies" too: both present ×5. |
| 4 | C4 titles + guard | **DONE** | `<title>GILD FDA decision: Bictegravir and Lenacapavir, Approved August 27, 2026</title>`; `<title>RARE FDA decision: DTX401 AAV gene therapy, Approved August 19, 2026</title>`. Root cause: `mark_event_pages_decided.py`'s title regex required the literal ` \| pdufa.bio` suffix, which `fix_meta_lengths.py` strips on the same run — so a page decided on a later day than its title was fitted never re-matched. Suffix made optional. Guard extended: `test_no_pending_title_over_decided_body` in `tests/test_no_past_target_pending_pages.py`; 0→1→0 below. |
| 5 | SRRK index caveat | **DONE** | `/pdufa/SRRK`: "Catalent Indiana" ×1, "fill-finish" ×1 (Aug 21 release linked in the block). Root cause: `build_pdufa_ticker_index.py` regenerates `/pdufa/{TICKER}` from a bare shell and ran **after** `inject_sponsor_caveat.py` in the workflow, so the block was injected then wiped every run — and the bare shell has neither `<h2 id="the-story">` nor `<!--FRESH:END-->` for the injector to anchor on. Workflow step moved after the index builder; injector gained a `</h1>` anchor. The 09-06 ack's claim that the index page carried it was wrong: it did locally at ack time and was lost in the next CI run. Guard: `tests/test_sponsor_caveat_rendered.py`, below. |

## Watcher leads since last slot
- `watch_fda_approvals.py` (09:22 PT): 52 armed events checked against FDA's feed, **0 unreviewed approvals** (3 previously reviewed, in the ledger).
- `watch_drug_approvals.py` (09:23 PT): 393 drug pages (255 already record an approval), 13 press items scanned, **0 unreviewed leads** (3 previously reviewed).
- `watch_readouts.py --dry-run` (09:24 PT): 182 pending readouts checked against ClinicalTrials.gov, **19 unreviewed registry signals** — the same advisory backlog first surfaced 09-02e (then 20), all Estimated-precision rows whose registry shows COMPLETED/TERMINATED or primary-completion ACTUAL (ARDX ×2, TRVI, AMGN tezepelumab, CALC, HOWL, SNGX, AKBA, BIVI, EYPT, JSPR ×2, AGMB, IMMP, ELTX, IXHL, FTH, HUMA, OGN). None verified or acked this slot: each needs the sponsor's own release read, and a bulk ack is what the 09-02e note forbade. No day-precision Guided readout is in scope. Nothing published this slot; nothing acked as a non-event.

## Root causes and corrections
- **C2**: the auditor's mechanism ("the header stamper uses the CI/UTC date") was right in effect but the clock was one file upstream — `build_sitemap.py`'s content-change date, not the stamper. Stated above.
- **C4**: mechanism was a regex/suffix ordering bug, not a missing rule. Stated above.
- **Item 5**: the 09-06 ack over-claimed; the index page was rendered locally and destroyed by CI step order. Corrected in the workflow and now guarded.
- **CI was broken before this slot started**: David's favicon commit a42ec7940 (20:55 PT Sept 6, after the 03:47Z stamp) added `build_favicon.py`, which imports PIL; the workflow's `pip install` had no pillow. My manual dispatch 34142696590 (16:19Z) died at "Favicon assets + canonical icon links" with `ModuleNotFoundError: No module named 'PIL'` — it deployed nothing (the site got c3eec4e82 from the push, not from CI). Fixed in 364aa861b (`pillow` added), re-dispatched as run 34143020141 at 09:23 PT. Also note: no 12:00Z scheduled run appears in today's run list; the 21:00Z run will be the first scheduled one on the fixed workflow. The 08:00 audit's "commit field the builder promised is live" was true but the three commits pushed at 20:49–20:55 PT (d515530a1, 15e4286c8, a42ec7940) were deployed by Vercel under a build-info still naming e76d00ac7 — `commit` only moves when `build_freshness_stamp.py` runs, i.e. in CI or by hand. I stamped by hand this time (59ba8f997); the daily CI does it on every successful run.
- **On the 08:20 silence**: I have no evidence the slot fired at all — no ack, no log, no process. The only trace is an uncommitted `_lib.mjs` edit (SWR 86400→300) with a comment dated today, which suggests a run started and died before writing anything. Stub-first is now in this ack's process (stub written 09:07 PT before any work).

## New guards
- `tests/test_api_cache_policy.py` → every `s-maxage` / `stale-while-revalidate` / `stale-if-error` in code (comments stripped) under `pdufa_site_src/api/v1/*.mjs` is ≤ 300 / 300 / 3600; fails if no directive is found. **0 → 1 → 0**: planted `stale-while-revalidate=86400` in `_lib.mjs` → "API cache policy allows a stale payload: _lib.mjs: stale-while-revalidate=86400 > 300" → restored → OK.
- `tests/test_one_date_per_page.py` → on `/calendar`, `/fda-this-month`, `/runup-by-year` and every `/drug/*`, all "Updated / updated / as of / Last computed" dates on a page agree; on the three daily pages that date equals `site_dates.eastern_today()`. **0 → 1 → 0, twice**: (a) run on the un-regenerated 03:47Z tree it reported "/calendar: 2 different stamp dates on one page: 2026-09-06, 2026-09-07" — the real defect — and OK after regenerating with the Eastern clock; (b) planted "Updated Sep 8, 2026" on `/runup-by-year` → "2 different stamp dates … 2026-09-07, 2026-09-08" → restored → OK.
- `tests/test_no_past_target_pending_pages.py::test_no_pending_title_over_decided_body` → a `<title>` matching `TICKER PDUFA date:` over a `<!--DECBAN:BEGIN-->`/`<!--DECIDED:BEGIN-->` banner whose own words say Approved/Complete Response fails. **0 → 1 → 0**: planted the old GILD title → "/pdufa/GILD-bictegravir-and-lenacapavi: <title>GILD PDUFA date: … Aug 27 2026</title> over a decided banner" → restored → OK.
- `tests/test_sponsor_caveat_rendered.py` → for every PDUFA dataset row with `_d.sponsor_caveat`, each existing event page (bare ticker and drug slug) contains the CAVEAT marker and the caveat's first 40 characters; fails if no such page exists. **0 → 1 → 0**: deleted the block from `/pdufa/SRRK/index.html` → "/pdufa/SRRK: sponsor caveat for SRRK not rendered ('Scholar Rock will continue to collaborat…')" → restored → OK.

## Fast currency checks (live, 09:21 PT)
API `meta.as_of` 2026-09-07, total 456; PDUFA rows 85; day-precision past-goal undecided **0**; past-date Guided without outcome **0**; `/calendar` "97 … 48 are still ahead, and 49 have been decided" → 48 + 49 = 97 ✓.

## Queued / deferred (with why)
- Planted-clock rendering for C2 (an injectable `SITE_BUILD_CLOCK` env read by `site_dates`) — not built this slot; the render guard covers the acceptance, the injectable clock would let it run at a fake 03:00Z in CI. Next slot.
- Source-level clock guard (no `date.today()` in builders): 90+ scripts still use it; a guard now would be red on day one. Migrate the daily-pipeline builders first, then guard.
- CARRY-FORWARD 3–9 from the 08:40 note untouched (NEW-4 API url split, JUVÉDERM PMA, torque item 7, UNCY case study, Gemini keepers + /terms /privacy, 13F/Form 4).
- Run 34143020141 (CI on the fixed workflow) was in progress at ack time; if it commits, `build-info.commit` will move again to a `chore: daily data refresh` SHA whose parent is 364aa861b.

*Informational and educational only; not investment advice. Builder, 09:24 PT.*
