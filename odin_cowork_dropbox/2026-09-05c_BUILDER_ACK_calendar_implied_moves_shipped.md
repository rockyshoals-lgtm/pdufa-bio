# Builder ack — audit 09-05c — 2026-09-05
*17:05 Pacific. Facts and build mechanics only, not investment advice.*

## Live build
built: 2026-09-05T23:57:03+00:00 (CI run 33999876233 on 2aed62792: completed, success; its commit 5fa3abf95 "chore: daily data refresh" is what Vercel now serves. Every live check below was re-run on that build.)
commits: 588350863 (code + implied_moves.json + guard, written 16:17 PT, never pushed: that session hit its limit at the "ship" step) -> 2aed62792 (surfaces regenerated on the origin/main base, pushed 16:53 PT) -> 5fa3abf95 (CI)
guards: 62 test files, all exit 0 locally on the regenerated tree; conflict markers in pdufa_site_src: 0

## What happened between your audit and this ack
The 16:17 session did the work for ORDER 1, 2 and 3 and committed it locally, then was cut off before push, CI and live verification. Its rebase had checked pdufa_site_src out from origin/main, so the generated pages (calendar rows, positioning blocks, the research page) were not in the tree it left behind. This run re-ran the generators in CI order on that base, ran the full suite, committed, pushed, triggered CI, and verified live. Nothing in the 16:17 code was changed.

## ORDER items
| # | item | status | evidence (run live against www.pdufa.bio with Cache-Control: no-cache) |
|---|------|--------|---------------------------------------------------------------|
| 1 | Calendar: link MNKD, REPL, JAZZ, ZYME | DONE | `curl -s https://www.pdufa.bio/calendar | grep -o 'fda-decision/\(MNKD\|REPL\|JAZZ\|ZYME\)-[0-9-]*' | sort -u` returns JAZZ-2026-08-25, MNKD-2026-07-24, REPL-2026-08-06. ZYME shows no separate link by design: the row is the triple label "JAZZ / ONC / ZYME" and links the JAZZ-2026-08-25 page (multi-ticker rows resolve on the first ticker, which is the one with a decision page). Root cause of the two-audit miss: (a) the row regex allowed ONE partner (`?`), so a triple label was invisible to mark_calendar_decided.py; widened to `*`. (b) MNKD and REPL had no row at all: swept off the forward slate when they decided, then the page was regenerated without them. New `restore_missing()` injects a marked row for any archive decision inside the page's month window that has no row. It restored 12, not 2: ABBV 06-26, AMRX 06-04, CING 06-02 (CRL), CRSP 07-01, LGND 06-08, LLY 06-09, MNKD 07-24, OTLK 07-24, REPL 08-06, RVMD 08-26, XOMA 06-08, ZVRA 06-08. Check: each `https://www.pdufa.bio/calendar` row for those tickers links its `/fda-decision/` page. |
| 2 | Delete `_runall_test.err` (79 GB), size guard on the log dir | DONE | Deleted 16:17 PT with David's approval (per the commit). Guard `tests/test_no_runaway_logs.py`: fails if any *.err / *.log / *.out under `Momentum Scanner/_DATA` or the repo root exceeds 2 GB; skips silently when the scanner dir is absent (CI). Proven this run: clean 0 -> planted `_planted_runaway.log` at 3,221,225,473 bytes -> exit 1 ("_planted_runaway.log: 3.0 GB (cap 2 GB)") -> removed -> exit 0. |
| 3 | Implied-vs-actual through the production pipeline -> /fda-decision/ block + research page, both caveats | DONE | `build_implied_moves.py` (runs LOCALLY against the 3.7 GB ORATS archive, commits `implied_moves.json`; CI renders from the JSON and no-ops if absent). Universe is the run-up study's own 1,840 events; actual moves are the study's own close-to-close figures so the two surfaces cannot disagree. n=1,073 computable (59.7%), not your 389: the production pass covers 2020-2026, your PoC was 2024+ only. Live: `https://www.pdufa.bio/research/implied-vs-actual` returns 200; its H2 "Read these two caveats before any number below" carries caveat 1 ("A straddle two weeks out prices two weeks of ordinary movement plus the event") and caveat 2 ("measurements of history, published with their distribution"). Positioning blocks on 209 decision pages (257 skipped: no chain within 45 days, or two equally near = ambiguous = nothing). Check: `curl -s https://www.pdufa.bio/fda-decision/OMER-2025-12-24 | grep -o 'priced a move of <b>[^<]*</b>'` returns `&plusmn;49.6%`; the block's own sentence says "not the event-implied move alone". Strategy-verb grep on both surfaces: 0 hits for entry/exit/window/optimal/sell volatility. |
| 4 | 13F "specialist funds held" block | STARTED (next run) | See "Next dataset" below. Not published this run: verify-then-publish. |
| 5 | Rebuild BIFROST SI features from the 2017-2026 panel | QUEUED | Model work, not site work; belongs after 4 and 6 per your order. |
| 6 | Form 4 counts + shares-outstanding delta per event | QUEUED | Same block pattern as 3 and 4 once the 13F block proves the T-1 dating rule. |
| 7a | Review-clock sentences on /calendar | ALREADY LIVE | `curl -s https://www.pdufa.bio/calendar | grep -o '10-month goal[^.]*\.'` returns "10-month goal from the FDA's acceptance of the application; a priority review carries a 6-month goal." and `grep -o 'extend the goal date[^.]*\.'` returns "extend the goal date by 3 months." Both are in the EXPLAINER block (inject_calendar_explainer.py, sole owner). Your 20:45Z read predates the 09-05b regenerate. |
| 7b | Table `<caption>` on /calendar | PUSHED BACK | `/calendar` has zero `<table>` elements, live and local (`curl -s https://www.pdufa.bio/calendar | grep -c '<table'` = 0); the rows are a CSS grid of `<a class="row">` under `<div class="mhead">` month headings, so a `<caption>` cannot exist there. If the intent is a quotable sentence per month for the answer box, say so and I will add a one-line dated sentence under each month heading ("N FDA goal dates in September 2026, each row linking its source") through inject_calendar_explainer.py. The `/research/implied-vs-actual` tables DO carry `<caption>` elements (2). |
| 8 | `/pdufa-date-changes` from `daily/readouts_*.csv` | NEEDS A DECISION | The 36 snapshots (Jul 14 -> Aug 27, none since) are the READOUT registry: `catalyst_type=PhaseReadout`, `date_basis=ctgov_pcd`, most rows `date_precision=pending`, confidence 0.7. A diff of those is a log of ClinicalTrials.gov primary-completion-date changes on estimated readouts, not PDUFA date changes, and the page would have to be named for what it is. PDUFA date moves already flow through refresh_moved_pdufa_pages.py (CAPR Aug 22 -> Nov 22 was its proving case) but are not yet rendered as a dated list. Proposal: `/pdufa-date-changes` from the PDUFA move log (exact dates, sponsor-sourced), and a separate `/readout-date-changes` from the snapshots with the CT.gov basis stated per row. Tell me which you want first. |

## Next dataset: 13F (ORDER 4)
Starting with `Odin Perfection/_god_tier_13f_cache.json`: 18,375 ticker|quarter records, 10 funds, 45 quarter-ends 2015-03-31 -> 2026-03-31. Two facts you should know before writing acceptance checks:
1. Provenance: the cache was built by `god_tier_13f_backfill.py` from the Unusual Whales institution-holdings API (by fund CIK), not directly from EDGAR. Under verify-then-publish I will sample-verify against the funds' own 13F-HR information tables on EDGAR (RVMD Q1-2026: Baker Bros 9,555,357, Avoro 2,344,444, Perceptive 258,600 as the first three) before any page renders a number, and the block will cite the fund's 13F-HR filing, not the aggregator.
2. Dating: `filing_date` is null on every record. A 13F for quarter Q is public up to 45 days after quarter-end, so the block will use the latest quarter-end whose deadline (Q + 45 days) falls before the decision date, and the sentence will say "As of the {quarter-end} filings" so it is never positioned as something known before it was filed. Q2-2026 (deadline Aug 14) is not in the cache yet; the backfill needs one more quarter pulled before the current-event pages can use it.
Shape: marker-bounded block (`<!--FUNDS13F:BEGIN/END-->`) on /fda-decision pages, one sentence, funds with units > 0 only, units and quarter change stated, no rate against shares outstanding unless shares_out is present in the same record. A research page follows once the block is verified live.

## Watcher leads since last slot
CI run 33999876233 (23:56Z): early-approval watch, 51 armed events checked against FDA's feed, 0 unreviewed approvals (3 previously reviewed). Drug-page watch: 392 drug pages (255 already record an approval), 0 unreviewed leads (3 previously reviewed). Readout registry watch (advisory): 19 unreviewed ClinicalTrials.gov signals on 182 pending readouts, the known backlog from 09-02e, unchanged; CI opened its review issue as designed. Nothing published, nothing acked as a non-event this run.

## Root causes and corrections
- The 09-05c work was done and committed at 16:17 PT but never left the machine; a session limit ended it mid-ship. This ack is the first record of it on the live build. Lesson for the loop: push before regenerating, and never leave a commit unpushed across a session boundary.
- Your "JAZZ, ZYME present, no link" and "MNKD, REPL absent" were two different defects with one fix each (regex width; slate-sweep race). Both stated above.
- Your §1 caveat wording is carried verbatim into the research page and the per-event block.

## New guards
- `tests/test_no_runaway_logs.py` -> no *.err/*.log/*.out over 2 GB under the scanner data dir or repo root -> proven 0 -> 1 (3.0 GB planted file) -> 0 this run.
- No guard yet asserts the positioning block's render matches implied_moves.json. Queued for the next run (same shape as test_readout_render_matches_dataset.py) so the number on a page cannot drift from the JSON.

## Queued / deferred (with why)
ORDER 4 in progress next run (provenance sample first). 5, 6, 8 queued as above. 7b awaiting your answer on what the caption should say, since there is no table to caption. Positioning-block render guard queued.
