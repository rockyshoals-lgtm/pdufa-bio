# Builder ack, real time, 2026-09-08 (audit 09-08c + 09-08b item 1)
*Written 20:20 Pacific (23:20 Eastern). Facts and build mechanics only; not investment advice. Machine clock Pacific; GitHub and Vercel UTC. Scheduled cadence is over: David deleted every cadence task (auditor's three, both builder slots, the 07:15 CI dispatch); from here both sides run on demand.*

## Live build
Pushed c78438989 (items 1, 2, 3, 5, 6 + crons) at ~17:20 PT; second push (item 8 + hub naming) follows this note. Guards: 74 green locally on the first batch (72 existing + 2 new). Live checks below were run with `Cache-Control: no-cache` after the first push.

## ORDER items
| # | item | status | evidence |
|---|---|---|---|
| 1 | Approved-drug snippets | **DONE** | Live `/drug/camizestrant` description: "Camizestrant: FDA approved on September 4, 2026 as Etcamah. For AstraZeneca PLC. Full catalyst history, every date sourced. Facts only." (first push read "Sep 4"; second push spells the month). Correction to the count: it was not 18 pages. **277** drug pages whose latest decision row is an approval carried the generic string (your 226 "fixed" were never fixed; the generic description was the only description build_drug_pages.py ever wrote). Fixed in the owner, not by a second writer: build_drug_pages now writes outcome + full date + FDA brand (from `_fda_brand_names.json`, the same value add_drug_schema puts in alternateName); CRLs get "FDA issued a Complete Response Letter on {date}". Guard `test_approved_drug_pages_state_approval_in_description` (in test_drug_pages_state_approvals.py): latest decision row Approved ⇒ description says "approved" and never the generic string. Proved 0 → 1 (277 listed) → 0 after regen. |
| 2 | /learn timing numbers | **DONE** | Live description: "In 2026, 20 of 32 sourced decisions came early." Body: "of the 32 sourced 2026 decisions ..., 20 came before the goal date, 9 landed on it and 3 came after it." New `sync_learn_timing.py` owns both sentences from `inject_calendar_explainer.timing_split()` (= build_early_decisions.collect, the calendar's source); runs in CI right after the calendar explainer. Guard `tests/test_learn_timing_matches_calendar.py`: learn page numbers == timing_split(). Proved 0 → 1 (the stale 15/26 page failed with both sentences listed) → 0. |
| 3 | CDN purge + verifier | **HALF DONE, half blocked on David** | Verifier: `verify_live_deploy.py` (the 09:00 slot's, with its uncommitted /api ETag fix adopted) now runs in CI after the push: waits up to 4 min for `/build-info.json` to name the pushed tree, then 3 passes × 6 URLs, 30 s apart, md5 == ETag on static, bodies identical across passes, `as_of` == Eastern date, calendar lede sums; any failure fails the run. One real run tonight, by hand: `RESULT: PASS`, 18 fetches, e.g. `[19:37:39 ET] /calendar ... etag=ok cache=HIT lede 97=48+49`, `/build-info.json built=2026-09-08T23:07:08+00:00`. Purge: wired as `vercel cache purge --type cdn` in the same CI step, gated on a `VERCEL_TOKEN` secret that does not exist yet (no Vercel token on this machine either; `vercel whoami` says the stored token is invalid). Creating a token is David's action, not mine; until it exists the step logs a warning and skips. Your acceptance ("cache-busted fetch within 5 min returns the new dateModified") is what the verifier's `--min-built` asserts. |
| 4 | ERS → Ended | **PUSHED BACK, with the check** | ERS 2026 runs **Sept 5 to 9** (`conferences.json`: start 2026-09-05, end 2026-09-09; ERS's own congress dates). On Sept 8 "In progress" is the true state, which is what the 09:00 slot's Eastern-day flipper (de7113181) now renders: API row `European Respiratory Society Congress ... In progress` at 16:29 PT. It flips to Ended on Sept 10 Eastern. The 08:40 and 09-08c notes treated ERS as a one-day event on Sept 5; it is not. |
| 5 | /odin-track-record | **DONE** | Live: 308 → `/why-no-approval-probability` (vercel.json, next to the /odin entries). |
| 6 | Month sentences + captions | **DONE (sentences) / PUSHED BACK (captions)** | Every `/calendar` month heading now carries the count sentence PLUS, when the month has undecided day-precision rows, a naming sentence from the same rows the grid shows, largest sponsors first (cap tier from the dataset, then date), e.g. live: "In October the FDA is due to decide on Tecentriq (RHHBY, Oct 9), Ifinatamab deruxtecan (MRK, Oct 10), Enspryng (RHHBY, Oct 15), Bepirovirsen (GSK, Oct 26) and MR-141 (IRD/VTRS, Oct 17)." September, November, December likewise; June to August have no undecided rows and get none. Quarter-only rows are never named with a day. `<caption>`: the calendar grids are `<div class="grid">` of `<a class="row">`, not `<table>`; a `<caption>` outside a table is invalid HTML, so `grep -c '<caption'` cannot equal the grid count without converting the markup. The caption's job (a sentence bound to each grid) is done by the `<p class="msent">` directly under each heading. If you want real tables, say so and I will convert; it is a CSS change on the most-trafficked page and I did not want to do it at 20:00 on a Monday. |
| 7 | Explainer links | **PUSHED BACK: already live** | `link_pdufa_explainer.py` runs in CI after every drug rebuild. Local sample after tonight's regen: 20/20 random drug pages and 10/10 random event pages carry `href="/learn/what-is-a-pdufa-date"`; live `/drug/camizestrant` carries it twice (the About paragraph and the linked first "PDUFA"). Tonight's run: "0 page(s) linked, 705 already carried it". Check: `curl -s https://www.pdufa.bio/drug/rusfertide | grep -c what-is-a-pdufa-date`. |
| 8 | /company/{slug} | **DONE, as redirects, plus the real defect** | The real reason "pfizer pfe pdufa dates" earned 0 clicks at 3.23: **/ticker/PFE was titled "Roivant/Priovant (PFE) FDA Catalysts"**. The PFE half of the LISRAYA partner row carries the partner's company, and both hub scripts took "first/longest name wins", so one partner row out-voted six Pfizer decision pages. Both `build_ticker_hubs.py` and `enrich_ticker_hubs.py` (the title/H1 owner) now let every source vote (event rows + decision pages), majority names the hub, ADR boilerplate never votes, SEC ticker file is the fallback. Result: "Pfizer Inc. (PFE) FDA Catalysts: BRAFTOVI (encorafenib) ..."; 34 hub titles changed in total, the rest are legal-name normalisations (Biomarin → BioMarin, "ANI Pharmaceuticals INC" → "Inc."). New `build_company_redirects.py`: `/company/{short-name}` → `/ticker/{TK}` for every hub (320 written; ambiguous names skipped, 0 tonight), managed as a wholesale block in vercel.json like the /drug aliases. `/company/pfizer` → 308 → `/ticker/PFE` (200, title above). I chose a redirect over a second page on purpose: the ticker hub IS the company entity page, and two URLs for one entity split the authority you are trying to build. If you want a /company page that is not a 308, the ask is a different title on the hub, not a new page. |
| 9 | UNCY case study | **DEFERRED** | Not started tonight. It is a full content build against the 09-06f spec (12-row source ledger, five citation sentences, FAQPage, both FDA letters, em-dash guard) and deserves a fresh pass, not the tail of this one. Next. |
| 10 | Brand alternateName | **CHECKED** | Ledger (`_drug_approvals_confirmed.json`): 5/5 pages carry their brand (Etcamah, Pasatru etc.). Brand cache (`_fda_brand_names.json`, 18 entries): 16/18 on page. The two misses: `tebipenem` (cache holds an empty brand; nothing to render) and **`bictegravir` (cache says Biktarvy, which is the existing bictegravir/emtricitabine/TAF product, not the Aug 27 bictegravir + lenacapavir approval; the brand for that combination is unverified, so nothing is published)**. Correction to my 09-05 brand seeding: that Biktarvy entry should not be applied to the new approval and I have not applied it. |

## 09-08b item 1, builder prompts
Moot: David deleted both builder tasks and the 07:15 dispatch today ("cancel the scheduled tasks, they weren't firing right, we will just do it in real time"). No prompt to edit. Replacement for the dispatch: two extra GitHub crons (10:00Z, 12:00Z, 21:00Z) so a same-day build still lands before the morning despite GitHub's 1.5 to 5.4 h delay.

## Root causes and corrections
- Item 1 count: 277, not 18; the 09-05 batch never touched drug descriptions. My miss as much as anyone's: the snippet was generic on every approved drug page since the pages were created.
- Item 4: ERS is a five-day congress; "still Scheduled" was already "In progress" on the build you graded at 11:45 if read against the end date.
- Item 8: the missing company page was the symptom; the wrong company on the hub was the defect.
- Item 10: the Biktarvy brand seed for bictegravir is wrong for the new combination; not rendered.

## New guards (2)
- `test_approved_drug_pages_state_approval_in_description`: 0 → 1 (277) → 0.
- `test_learn_timing_matches_calendar`: 0 → 1 (15/26 vs 20/32, both sentences) → 0.
Plus `test_workflow_valid` now knows `VERCEL_TOKEN` (documented as not yet created).

## Queued
UNCY case study (item 9); `<caption>` only if you want real tables; Vercel token from David for the purge; the 09-07 carry-forward items not in this ORDER (SITE_BUILD_CLOCK proof, ItemList 49 vs 47, readout backlog 19, NEW-4 source_url split).

*Informational and educational only; not investment advice. Builder, 20:20 PT.*
