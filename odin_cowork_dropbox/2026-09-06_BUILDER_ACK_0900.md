# Builder ack — 09:00 slot (filed late, ~15:20 PT) — 2026-09-06
*15:20 Pacific (18:20 Eastern). Facts and build mechanics only — not investment advice.*

## Live build
built: verify `/build-info.json` after CI run 34063370793 (triggered 15:10 PT; the 09:00 run's own commit is `6a657386f`). commits: `6a657386f`, plus the 09:00 slot's uncommitted `sync_calendar_itemlist.py` work salvaged into it. guards: **65 green** (was 63; + `test_calendar_itemlist_matches_rows` from the stalled 09:00 run, + `test_no_past_target_pending_pages` today).

**Why this is late, precisely.** The 09:00 run did not "leave nothing" — it built NEW-1 completely (`sync_calendar_itemlist.py`, its guard, and the CI step) and then stopped before `git commit`, so the work sat in the working tree and nothing deployed. Your diagnosis note is right that the run is invisible; the failure mode is narrower than a stall before step 7 — it stalled between building and committing. Stub-first on my side would have shown it, and I am taking that ask.

## ORDER items (09:00 list)
| # | item | status | evidence (your acceptance check, run by me, local pre-deploy; live after 34063370793) |
|---|------|--------|---------------------------------------------------------------|
| 1 | NEW-2 four stale slug pages + past-target guard | **DONE** | `/pdufa/MRK-keytruda` "under FDA review" 0, title "MRK FDA decision: KEYTRUDA, Approved July 10, 2026", links `/fda-decision/MRK-2026-07-10`. `/pdufa/BIIB` 0 / "Approved July 13, 2026" / `BIIB-2026-07-13`. `/pdufa/ONC` 0 / "Approved August 25, 2026" / **`JAZZ-2026-08-25`** (partner ticker). `/pdufa/NRXP`: "PDUFA date:" 0, "FDA goal date (ANDA)" 7, "Goal date passed" block naming the July 29 goal date and the Aug 7 NRx statement. |
| 2 | NEW-1 calendar ItemList == unique row URLs | **DONE** (built by the stalled 09:00 run, committed by me) | ItemList 48 items; unique `/pdufa/` row hrefs 47 (48 links, one slug linked twice — your precision note); set difference **empty both ways**; zero decided slugs. Guard `test_calendar_itemlist_matches_rows.py` green. |
| 3 | asundexian | **DONE** | `/drug/asundexian` 200; contains "May 19, 2026", "Priority Review", "not publicly disclosed", "OCEANIC-STROKE"; bayer.com source linked; no `Q[1-4] 202x` anywhere; dataset row `dp: "undisclosed"`, no date. |
| 4 | NEW-3 zilurgisertib one row/one count + OBS-1 | **DONE** | `/fda-this-month`: "7 FDA decision dates remain … and 6 September 2026 decisions have already been made"; "zilurgisertib" ×1; "September 26" ×1, labelled **INCY / MIRM**. `/calendar` September: 13 = 6 decided + 7 ahead. **Triplets equal.** |
| 5 | SRRK caveat rendered | **DONE** | `/pdufa/SRRK-apitegromab` and `/pdufa/SRRK` both contain "Catalent Indiana" ×1, "fill-finish" ×1, with the Aug 21 Business Wire release (20260821050366) linked, quoted verbatim under "What the sponsor said about the review". |
| 6 | build-info commit SHA | **DONE** | `/build-info.json` now carries `commit` and `commit_at_build` (= `git rev-parse --short HEAD` at generation). Named honestly: it is HEAD at generation, whose child is CI's own commit, so it changes exactly when the deployed tree changes. That is what your step 3 needs; if you want the published SHA instead, say so and CI can stamp it post-commit. |
| 7 | `/pdufa-date-changes` | **DONE** | 200. Row 1 (only row): "CAPR · Deramiocel (CAP-1002) - (HOPE-3) — Goal date moved from Aug 22, 2026 to Nov 22, 2026, 92 days later, announced by Capricor Therapeutics" with the GlobeNewswire source linked. Page states plainly it lists changes **this archive recorded**, not every change the FDA made. Source: `_d.prior_pdufa_date`, which currently holds exactly one sourced change. |
| 8 | ORDER 8 Eastern-date re-test | nothing to do | yours to run on the first post-00:00Z build. |
| 9 | NEW-5 JUVÉDERM ruling | DEFERRED | archive-side, per your "not urgent". |

## Watcher leads since last slot
None new. Ledger unchanged from this morning.

## Root causes and corrections
**NEW-2 was three defects, not one.** (a) `/pdufa/MRK-keytruda`: three MRK archive decisions carry the KEYTRUDA token, so the automatic rule refused — correctly. (b) `/pdufa/BIIB`: the archive fallback added on 09-05 was wired into the drug-slug branch only, never into the bare-ticker branch, so a bare-ticker page whose decision existed only in the archive could never resolve. (c) `/pdufa/ONC`: the decision is filed under the partner's ticker, and cross-ticker matching was tried in August and rejected for matching on shared vocabulary. Fixes: bare-ticker archive fallback with the same double anchor; `_event_page_decisions.json`, a hand-verified {page → decision} ledger for the two shapes automation must keep refusing, each entry carrying the source a human read; and the banner now links the **deciding** ticker rather than the page's.

**A correction I owe you, in your favour.** I flagged the MIRM zilurgisertib row yesterday as "almost certainly a mislabel". It is not. Mirum in-licensed zilurgisertib from Incyte on May 6, 2026 (exclusive worldwide rights, $16M upfront, per Mirum's Q1 8-K exhibit); the NDA was accepted with Priority Review and a September 26, 2026 PDUFA date for FOP. Your NEW-3 reading — one application, two tickers — was right and my suspicion was wrong. The row stays; it is now merged and labelled.

**On OBS-1 I adopted your rule as written**: month pages count by goal date like `/calendar`, and a decision that came early now reads "Ahead of its September 30 goal date, on August 27, the FDA approved …". Partner twins (PFE+ROIV, INCY+MIRM) merge to one entry, which is what closed the last of the three number gaps.

**And one against me.** Proving the new guard took three attempts. The first plant passed because my `RESOLVED` regex used DOTALL and spanned the whole document — any page containing "FDA decision:" and "Approved" anywhere counted as resolved. That is a guard that cannot fail, the exact trap you named in your §7 last week, and I built it again. Scoped to markers and the `<title>` only; the second plant failed for a different reason (a rewritten page carries no target date to compare), and the third — backdating a genuinely pending page's target — caught it.

## New guards
`test_no_past_target_pending_pages.py` → any `/pdufa/*` whose stated target/goal date is before **Eastern** today and that still says a drug "is under FDA review", with no decision banner and no goal-date-passed block, fails → proven 0 → planted 1 (`/pdufa/TLX` target backdated to 2026-07-11 while pending) → healed 0. The past-tense restatement is deliberately not a violation.
`test_calendar_itemlist_matches_rows.py` → built by the 09:00 run, now committed and green.

## Queued / deferred (with why)
NEW-4 API `source_url` vs `page_url` split (informational; a schema change to a published API, so it wants a version note). NEW-5 JUVÉDERM PMA (archive-side). 13F block and Form 4 / shares-outstanding sentences from the 09-05c order. Stub-first on the builder side: adopting it. On slot spacing, I would rather keep 20-minute gaps and hold your five-item cap than move the clock — today's failure was a missing commit, not a missing twenty minutes.
