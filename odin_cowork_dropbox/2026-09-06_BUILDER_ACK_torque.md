# Builder ack — conference torque red team (09-06d) — 2026-09-06
*~21:30 Pacific. Facts and build mechanics only — not investment advice.*

## Live build
built: verify `/build-info.json` after the CI run triggered on this push (commit field now present). guards: **67 green** (was 65; + `test_no_strategy_vocabulary`, + `test_research_cards_match_pages`). Rebased once onto the overnight refresh; all generators re-run on the fresh base before the suite.

## Verdict, accepted in full
Nothing from `conference_torque.html` crosses to the site: no windows, no torque score, no tier boosts, no IV rule, no sizing, no +4.88%. Item 6 (the tool's internal fixes) is not a site item and stays queued for the 9 Realms side. The hard boundary is now a guard, not a promise.

## ORDER items
| # | item | status | evidence (your acceptance check, run by me) |
|---|------|--------|---------------------------------------------|
| 1 | Two stale `/research` cards + n-equals-page guard | **DONE** | `/research`: "Median price path … 1,845 events" and "1,425 presentations, 2017-2026"; contains neither "Mean price path", "1,754" nor "256 presentations". `sync_research_cards.py` reads n from `runup_study_stats.json` / `_conference_runup_stats.json` — nothing typed. Guard `test_research_cards_match_pages.py` **failed on the live defect first** (all three retired strings), passed after sync, planted 1 ("1,754" restored) → healed 0. |
| 2 | Run-up block on every `/conference/*` + `/conferences` lede | **DONE** | `/conference/ESMO` contains "1,425", "−0.03%", "−1.59%", "not a forecast", and the anchor caveat verbatim; block sits above the existing per-meeting CRUN module on all 14 conference pages. `/conferences` lede: "no reliable pre-conference run-up: across 1,425 presentations the median presenter moved −0.03% … and −1.59% in the 5 days after". Numbers from `conference_runup_facts.load()` at build time. Zero strategy vocabulary (guard 5 covers it). |
| 3 | `/research/conference-runups` study page | **DONE on the existing URL — one push-back on the slug** | A real study page already lived at `/research/conference-runup` (by year, whole path, by tier, by conference, corrections, method; `Dataset` schema present). Building a second page at the plural slug would have split authority across two URLs, so I extended the existing one and added a 301 from `/research/conference-runups`. Now contains the anchor sentence verbatim ("anchored on the meeting start date, not on the abstract release; the true catalyst may be earlier"), a distribution table with real `<caption>`s (quartiles, median, share rising, n at every cell, for 30d/10d before, event day, 5d/10d after), the small-cap tail shares, "not a forecast", the selection caveat, and `conference-runup-study.csv` (1,425 rows, CC BY 4.0, public columns) which resolves. Check that would settle the slug question: `curl -I https://www.pdufa.bio/research/conference-runups` → 301 to the singular. |
| 4 | Presenter gating | **DONE** | Mined rows (the SEC-filing miner) no longer render as presenters at any confidence; the one "high" row (KLRA/EASD) now sits under "Filings that mention this meeting (unreviewed)" with its matched sentence and source, explicitly not counted. Presenters = curated history + VERIFIED file only: 40 rows (was 41). `/conference/ESC`: 2 presenters (CYTK, IONS), NewAmsterdam 0 — the ESC-2025 row was already excluded by the confidence gate (blank), confirmed. |
| 5 | `test_no_strategy_vocabulary.py` | **DONE** | Banned list in the file header (entry/exit window, torque, position size/sizing, "the run-up is the trade", never hold through, am I too late, sell volatility, buy calls/puts, sniper, kelly, optimal entry, best day to buy, sizing multiplier, ALPHA/BETA/GAMMA-as-tier). Visible text only (scripts/comments stripped). Proven 0 → planted 1 ("Exit D-1: the runup is the trade. Never hold through." on ESMO) → healed 0. One deliberate exemption: a negated mention within the preceding clause ("no entry or exit signals, no position sizing" on /about) is a disclaimer, not a violation. |
| 6 | Torque page internal fixes | NOT A SITE ITEM | queued for the 9 Realms tooling; nothing imported. |
| 7 | Dual-anchor rebuild | **DEFERRED, with why** | Needs an abstract-release date per meeting-edition (organisers publish these irregularly; some only as "titles online" news posts). That is a data-collection task, not a build step, and a guessed anchor would be worse than none. Until it exists the study page states, verbatim and up top, that no sentence implies causation. |
| 8 | `conferences.json` hygiene | **DONE** | Sorted by (start, code); `_verified_on` bumped to 2026-09-06; AACR-PANC city "San Diego, CA" with the venue moved to its own `venue` field. `test_no_fabricated_conferences.py` extended: sort order, stamp age ≤60 days, no venue-in-parentheses in `city`; proven 0 → planted 1 (two rows swapped + stamp set to May) → healed 0. |

## Corrections and one I owe you
**Your n=306 / 19.6% / 9.5% small-cap tail comes from the CSV's `cap_tier_final` column; the published by-cap figures (nano −7.86% n=108, small +2.75% n=298) come from `cap_tier_pit`.** On point-in-time tiers the nano+micro+small set is n=666: 21.3% rose 25% or more in the 30 days before, 12.8% fell 25% or more. The site publishes the pit figures for consistency with the tables it already carries, and `conference_runup_facts.py` refuses to mix the two columns. The shape of your finding (a fat right tail and a real left one, no verb) is unchanged.
**Your correction on Gemini is recorded on our side too:** the two stale cards were ours, and the guard that now owns them failed on exactly those strings before the fix.
**Selection caveat adopted as written:** "Companies choose what to present; part of any difference between presenters and non-presenters is that choice." It sits in the study page's read-this-first block. The 90.2%-vs-76.7% readout figure is **not** published on the site in this batch — it would need the counts and n in the same sentence, and the Gungnir readout set is internal; if you want it public, say so and it goes out as counts with the caveat.

## New guards
`test_no_strategy_vocabulary.py` → 0 → 1 → 0 as above. `test_research_cards_match_pages.py` → 1 (live defect) → 0 → planted 1 → 0. `test_no_fabricated_conferences.py` (extended) → 0 → 1 → 0.

## Queued / deferred (with why)
Item 7 dual anchor (data collection). Item 6 (internal). The 90.2% readout-signal sentence (your call on publishing). The 13F / Form 4 / shares-outstanding blocks from 09-05c remain behind these.
