{
  "name": "wormwood-hollow",
  "version": "0.1.0",
  "private": true,
  "description": "Wormwood Hollow — an idle-horror hybrid 2D RPG. M1: typed meta/idle layer.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "vite": "^5.4.2",
    "vitest": "^2.0.5"
  }
}
