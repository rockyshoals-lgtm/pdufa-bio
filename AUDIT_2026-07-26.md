# Audit — 2026-07-26
*Live API + rendered pages + primary-source verification. Not investment advice.*

**Short audit: last session's two P0 API bugs are fixed and verified. One substantive item remains (bulk refresh), plus two minors. No regressions.**

---

# ✅ P0 — API decision-capture is fixed (verified against primary sources)

The 07-25 finding was that the API published MannKind's approval as a self-contradictory record: `Decided`, no `outcome`, scheduled date, `days_to_decision: 1`. **All three are corrected:**

```json
// MNKD FUROSCIX ReadyFlow
{ "status":"Decided", "outcome":"Approved",
  "decision_date":"2026-07-24", "days_to_decision":null }
```

- **`outcome` is now exposed** — the single most valuable missing field is present. Every `Decided` event in the API now carries an outcome (0 missing across the whole set).
- **`decision_date` added** (7/24, the real approval) alongside the scheduled `date` — correct pattern.
- **`days_to_decision` recomputed to `null`** on Decided — the "decided but +1 day to go" contradiction is gone. Swept the full set: **0 Decided events with positive days_to_decision.**

**And the OTSKY gap closed too:** centanafadine flipped from the stale "past-dated Upcoming" to `Decided / Approved / decision_date 2026-07-24`. **0 past-dated Upcoming PDUFAs remain.**

### Both approvals independently verified — the data is correct
- **MNKD FUROSCIX ReadyFlow — Approved** (announced 7/24). ✅
- **OTSKY centanafadine (SIMTRIYO) — Approved** (announced 7/25; first-in-class NDSRI for ADHD). ✅ I checked this specifically because it's a live "Approved" claim on the site — it's accurate.

This was the right fix and it's complete. The API and the rendered pages now agree, which matters because `/llms.txt` sends crawlers to the API.

---

# 🔴 Still open (unchanged): the bulk refresh — day 15

```
2026-07-26 : 59 rows   ← today (near-term + decided)
2026-07-25 :  1
2026-07-24 :  1
2026-07-11 : 326 rows  ← still frozen → mode → badge "Data through Jul 11"
```

The daily job clearly refreshes **near-term and just-decided** events (MNKD/OTSKY captured within hours), but the **326-row bulk — mostly future readouts and later-2026 PDUFAs — hasn't rebuilt since July 11.** The badge honestly reads "Data through Jul 11" with the amber dot, so there's no dishonesty; but for the "most current source" goal, 84% of the corpus is 15 days old. **This remains the #1 item.** The near-term pipeline works — it needs to be pointed at the whole dataset, not just the front of the calendar. (Polygon feed.)

---

# 🟡 Two minors

1. **Event `startDate` is still date-only** (`2026-07-29`, no time/timezone). I flagged this 07-23 as the likely driver of GSC's "94% of Events not eligible for rich results." Not yet applied. Adding a nominal time + TZ offset (e.g. `2026-07-29T00:00:00-04:00`) is the fix; then let GSC recrawl. `VirtualLocation` + `OnlineEventAttendanceMode` remain coherent — no change needed there.
2. **Decided archive sorts by scheduled date, not decision date.** MNKD shows as the "most recent" decision at **2026-07-26** (its scheduled PDUFA) even though it was decided **7/24** — because the sort key is `date`, not the new `decision_date`. Now that `decision_date` exists, sort/display the archive by it so "recently decided" reflects when things were actually decided. Cosmetic, but it's on the homepage.

---

# ✅ No regressions
Fan-out join artifacts **0** · null drug names **0** · every Decided event has an outcome · sitemap 520 www-only · currency-honesty layer intact (mode badge + amber dot).

# 🟢 SEO (from this session's work)
8 more URLs queued for indexing (ABBV/AMGN/REGN/SRPT/BNTX + December calendar + /condition/cancer + /condition/rare-disease). `/learn/what-is-a-pdufa-date` already indexed — learn pages converting. GSC insight flipped positive: `/decisions` now gaining impressions. Clicks 42→44. (Note: GSC throttles URL inspection after ~6 rapid requests — cap batches at 5–6.)

---

# The order
1. 🔴 **Finish the bulk refresh** (326 rows / Jul 11). The only substantive open item. Point the working daily job at the full dataset.
2. 🟡 Add time+TZ to Event `startDate`; re-run Rich Results Test.
3. 🟡 Sort the decided archive by `decision_date`.
4. 🟢 Let indexing keep landing.

**One sentence:** the decision-capture pipeline now correctly publishes approvals — outcome, real decision date, no contradictions, both verified against the FDA record — so the remaining gap is purely that 84% of the dataset still hasn't been rebuilt since July 11, and that one operational fix is now the whole distance to the "most current" claim.

---
*Facts and historical statistics only. Not investment advice.*

Sources: centanafadine/SIMTRIYO FDA approval — https://finance.yahoo.com/healthcare/articles/otsuka-receives-fda-approval-first-012500452.html · MannKind FUROSCIX ReadyFlow approval — https://www.rttnews.com/3670514/mannkind-wins-fda-nod-for-furoscix-readyflow-announces-50-mln-private-placement.aspx
