# Audit — 2026-07-31
*Method: live rendered pages with `Cache-Control: no-cache` + cache-buster, live `/api/v1/events`, and primary sources. Current date 2026-07-31. Facts and historical statistics only — not investment advice.*

---

## 0. Currency check (the site IS current)
- Homepage freshness badge: **"Data through Jul 31"** — accurate (forward PDUFA rows stamped 07-30/07-31).
- Live API `meta.as_of`: **2026-07-31**, total 388 events.
- Rendered decisions are current: homepage "Recently decided" + `/decisions` show MNKD (07-24 ✓), OTLK (07-24 ✓ LYTENAVA), OTSKY (07-24 ✓), MRK Lipfendra (07-16 ✓); CAPR AdComm reflected. All verified on cache-busted pages.

---

## 1. 🟡 NEW — VTRS / Gwyn Lo approval not yet live on the site
- **Fact (primary source):** Viatris (Nasdaq: VTRS) received FDA approval on **2026-07-29** for **Gwyn Lo** (norelgestromin + ethinyl estradiol transdermal system) — a once-weekly, low-dose-estrogen combined hormonal contraceptive patch. 505(b)(2) pathway; supported by the Phase 3 **Luminous** study (**NCT05139121**); indicated for prevention of pregnancy in women with BMI < 30 kg/m². This is Viatris' internal candidate **MR-100A-01**, whose PDUFA we carried at 2026-07-30 (FDA acted one day early).
  - Source: Viatris newsroom / PRNewswire, "Viatris Receives U.S. FDA Approval for Gwyn Lo, a Once-Weekly Contraceptive Patch," Jul 29 2026.
- **Live-site state at time of audit (cache-busted):** NOT yet reflected.
  - Homepage still lists **VTRS in _upcoming_** (first tile); "Recently decided" still leads MNKD/OTLK.
  - `/decisions` still leads MNKD 07-24; no VTRS row.
  - `/fda-decision/VTRS-2026-07-29` → **404**.
  - API: VTRS `status:"Awaiting", outcome:null`.
- **Status:** The publish was prepared in the working tree (data.js forward record removed; dataset.mjs → Decided/Approved w/ FDA URL; `/decisions` row + counter→129; new `/fda-decision/VTRS-2026-07-29` detail page; calendar row marked; homepage board regenerated). **All 14/14 CI guards pass.** Owner's builder is handling the commit/deploy. → **Action: land the deploy so the four surfaces (homepage board, /decisions, /calendar, detail page) + API flip to Approved.**

---

## 2. 🔴 RECURRING — `/api/v1/events` still lags the rendered pages (now 3 live examples)
Fresh origin read of the live API vs the (correct) rendered site:

| Event | Rendered site | `/api/v1/events` |
|---|---|---|
| OTLK bevacizumab (LYTENAVA) | Approved 2026-07-24 | `status:"Awaiting", outcome:null` (upd 07-27) |
| CAPR Deramiocel AdComm | Held 2026-07-29, voted against | `status:"Scheduled", outcome:null` (upd 07-11) |
| VTRS Gwyn Lo | Approved 2026-07-29 (pending deploy) | `status:"Awaiting", outcome:null` (upd 07-30) |
| MNKD FUROSCIX | Approved 07-24 | ✅ `Decided / Approved` |
| OTSKY centanafadine | Approved 07-24 | ✅ `Decided / Approved` |
| MRK Lipfendra | Approved 07-16 | ✅ `Decided / Approved` |

**Pattern confirmed:** decisions written through the standard pipeline (which updates the API dataset `dataset.mjs`) appear correctly in the API (MNKD/OTSKY/MRK). Decisions published via the manual/AdComm path (OTLK, the CAPR AdComm) update the pages + decisions archive **but not the API serializer**, so the API reports them as still pending.
**Why it matters:** `/llms.txt` points ChatGPT/Perplexity/Gemini at this API — they read "OTLK awaiting" for a drug approved a week ago, and will read the same for VTRS/CAPR.
**Fix:** back-fill `status/outcome/decision_date` (+ bump `updated_at`) for **OTLK** and the **CAPR AdComm** in the API dataset, and make the **VTRS** deploy carry its dataset.mjs change (already staged). Longer-term: have the manual/AdComm publish path write the API record in the same step (it already writes the page + decisions archive).

---

## 3. 🟠 OPEN — Event schema still invalid on calendar/readouts
- `/calendar`: **54 invalid** Event items (was 55 on 07-30).
- `/readouts`: **150 invalid** Event items (unchanged).
- `/conferences`: **14 valid** (only non-critical warnings) — the real conference events validate fine.
- Root (unchanged): PDUFA/readout Event objects emit a **date-only `startDate`** + `VirtualLocation`. Fix = add time + timezone to `startDate` on that emitter (or demote undatable rows to `WebPage`). Clears ~204 invalid items and unlocks date-chip rich results on the two biggest hub pages.

---

## 4. 🟢 SEO indexing — recrawls requested today
All returned "URL is on Google / indexed"; recrawl queued for each:
- This round: `/decisions`, `/calendar`, `/conferences`, `/readouts`, `/screener`.
- Earlier today: `/` (homepage), `/research/conference-runup`, `/research/readout-reaction`, `/adcomm`, `/runup-by-year`.
- `/sitemap.xml` newest `<lastmod>` = **2026-07-24** — slightly stale; the daily rebuild should bump lastmod so recrawls see fresh dates. Worth confirming the sitemap regen step is running.

---

## The order
1. 🔴 **Land the VTRS deploy** (4 page surfaces + API) **and extend the API mirror to OTLK + the CAPR AdComm.** One fix family: make the API dataset a faithful mirror of the decision data the pages already hold. Clears the LLM-facing staleness for all three.
2. 🟠 PDUFA/readout Event `startDate` time+TZ → clears ~204 invalid schema items on /calendar + /readouts.
3. 🟢 Confirm the daily sitemap `lastmod` refresh is running (newest is Jul 24).

**One sentence:** the rendered site is current and correct, VTRS/Gwyn Lo just needs its already-prepared publish deployed, and the one durable bug remains the `/api/v1/events` feed lagging the pages for manually-published decisions (OTLK, CAPR, and VTRS until it deploys) — which is exactly what AI crawlers read.

---
*Facts and historical statistics only. Not investment advice.*
Sources: Viatris — "Viatris Receives U.S. FDA Approval for Gwyn Lo, a Once-Weekly Contraceptive Patch" (Jul 29 2026), https://newsroom.viatris.com/2026-07-29-Viatris-Receives-U-S-FDA-Approval-for-Gwyn-Lo-TM-,-a-Once-Weekly-Contraceptive-Patch
