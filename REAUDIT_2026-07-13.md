# Re-Audit — 2026-07-13
*Live + data, hard cache-bypass. Not investment advice.*

---

# 🔴 STOP — the CSV "fix" silently destroyed 69% of the conference data

**This is the finding of the pass. Do not publish anything conference-related until it's reverted.**

## What happened
The corrupt CSV (unterminated quote, break at **row 225**) now parses. But it parses because **the file was truncated at the break point, not because the escaping was fixed.**

| File | rows | tickers | **conferences** | date range |
|---|---|---|---|---|
| **`conference_presentations_history.csv`** (canonical, 07-13 15:47) | **224** | 117 | **11** | 2022-05-26 → 2026-12-12 |
| `...pre_rebuild_20260712_160021.csv` (backup, 07-12 23:00) | **715** | 240 | **39** | 2021-10-07 → 2026-12-12 |

**The canonical file is a strict subset of the backup — overlap 224/224. The backup holds 491 rows the canonical lost.**

- **69% of rows gone.**
- **28 of 39 conferences gone** (11 remain: ASCO, ASH, AACR, ESMO + 7).
- Early history gone (2021-10 → 2022-05).
- **The file is exactly 224 rows and the corruption was at row 225.** That is truncation, not a repair.

## Why it matters
**This undoes the entire 15 → 40 conference expansion the builder just completed.** EASL, ASGCT, EULAR, ATS, ASCO-GI, ASCO-GU, the CNS block (CTAD/AAIC/ECTRIMS/ADPD), AAAAI, ENDO, AAO, AAD, ARVO — all the newly-added first-class searches — **are not in the canonical output.** That was the highest-value data work of the week and it's been silently reverted.

## ✅ Not yet published — caught in time
Live check: `/api/v1/events?type=Conference` still returns **14 events**, and `/conferences` shows 14. **The truncated file has not reached production.**

## The fix
1. **Restore from `conference_presentations_history.pre_rebuild_20260712_160021.csv`** — 715 rows, 39 conferences, and it passes every integrity check: **fabricated = 0 · duplicates = 0 · ANE = 0 · null tickers = 0.** It is both **clean and complete.**
2. **Re-apply the quote fix properly** — `QUOTE_ALL` + sanitise `snippet` (strip newlines, normalise quotes) **before** writing. Do not truncate.
3. 🆕 **Add a coverage-regression CI guard.** This is the real lesson — a data fix destroyed two-thirds of the dataset and *nothing caught it*:
```python
# tests/test_crawler_no_regression.py — block deploy
import pandas as pd, sys
new = pd.read_csv('catalysts_out/conference_presentations_history.csv')
prev = pd.read_csv('catalysts_out/conference_presentations_history.prev.csv')
errs = []
if len(new) < len(prev) * 0.95:
    errs.append(f'FATAL: row count collapsed {len(prev)} -> {len(new)}')
if new['conference'].nunique() < prev['conference'].nunique():
    lost = set(prev['conference']) - set(new['conference'])
    errs.append(f'FATAL: lost {len(lost)} conferences: {sorted(lost)[:12]}')
if new['ticker'].nunique() < prev['ticker'].nunique() * 0.95:
    errs.append('FATAL: ticker coverage collapsed')
if errs: print('\n'.join(errs)); sys.exit(1)
print(f'OK — {len(new)} rows, {new.conference.nunique()} conferences, no regression.')
```
> **A crawl that returns fewer rows or fewer conferences than the previous run must fail the build.** Data can only be corrected, never silently reduced.

---

# ✅ Genuinely fixed this pass

| Item | Status |
|---|---|
| **P0-2 — fabrication** | ✅ **0 fabricated · 0 duplicates · 0 ANE** in the canonical file *(and in the 715-row backup)* |
| **P1-1 — unified panel dupes** | ✅ **202 → 0** (5,487 → 5,285 rows) |
| **`/glossary`** | ✅ now **200** |
| Site health | ✅ all routes 200 · sitemap **338 URLs, 100% www** · homepage mobile nav present · payments locked · API clean |

---

# 🔴 Still open — unchanged

| # | Item | Evidence |
|---|---|---|
| **P0-3** | **Do NOT rebuild the study from EDGAR** | Recall **53.5%** (AACR); **38%** for >$2B vs 57% <$2B. Published study is 49% large-cap — a rebuild silently re-weights it. |
| **P0-4** | **CLAUDE.md still mandates ODIN v14** | Live MCP: v14 = *"KNOWN LEAKED, inflated ~368bp"*; champion is **v19-PRUNE** (AUC 0.8934). **Owner action.** |
| **P0-5** | **ODIN retrain** | `prior_crl_count` **still max 26** in the live CSV. Capping the file alone changes nothing — coefficients were fit on uncapped data. CRL signal compressed to ⅓. |
| **P1-2** | **SI display cap** | 19% of rows have DTC > 100 days. *"4,200 days to cover"* on a page destroys credibility. Cap display at 60d → "very illiquid". Always print settlement date. |
| **P1-3** | ⭐ **`/ticker/{TICKER}` hubs** | Still **404**. Biggest SEO lever left; still absent from top 10 for the head term. `/drug/{name}` too. |
| **P1-4** | **BIFROST SI rebuild** | Still lookahead-biased; also makes `explosion_score` unusable. Panel is ready. |
| **P2** | `/watchlist` 404 · CRL tracker reframe · market-cap null · real-device mobile QA · Stripe/Resend keys | |

---

# Priority for the builder
1. 🔴 **Restore the 715-row backup. Re-apply the quote fix without truncating. Ship the coverage-regression guard.**
2. 🔴 **Don't rebuild `/research/conference-runup` from EDGAR** — multi-source first (EDGAR + `fmp_press` + abstract DBs), or publish old/new side-by-side with the recall figure.
3. 🔴 **Update CLAUDE.md** (v14 → v19-PRUNE). *Owner.*
4. 🟠 ODIN retrain on capped `prior_crl_count`.
5. 🟠 SI display cap.
6. ⭐ `/ticker/{TICKER}` hubs.

**The pattern worth naming:** every serious incident this week — the fabricated conferences, the corrupt CSV, and now the truncation — came from the **data pipeline**, not the site. The site is in good shape and well-guarded by CI. **The pipeline needs the same guards the site already has.**

---
*Facts and historical statistics only. Not investment advice.*
