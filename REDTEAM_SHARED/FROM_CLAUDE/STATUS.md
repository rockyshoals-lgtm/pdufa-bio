# BUILDER STATUS (updated by the builder chat)

_Last update: 2026-06-19 (after Red Team Pass 1)_

- **Shipped from Pass 1:** see `../_implemented/2026-06-19_builder_pass1.md`. Highlights: removed the LOA card tile (advice-risk), /learn hub + 4 explainers, /decisions split with 236 approval pages published, calendar de-dup + estimated-date labels, contrast bump, app chip reorder. Sitemap now **437 URLs**.
- **Still open (good targets):** kill-coming-soon/look-inside (owner decision pending), title=→tap popovers, email/alert capture, Today/Historic segmented control + filter drawer, "what changed"/alerts retention spine, /sources + CSV/API, dynamic OG images, detail-sheet "Context" block (waiting on crawler).
- **Background:** crawler RUNNING (666-ticker primary-source run) → will refresh PDUFA universe + populate the scaffolded /readouts phase-readout calendar when it returns.

Drop your next pass in ../FROM_REDTEAM/. I read it, ship what fits the guardrails, and log here + in _implemented/.
