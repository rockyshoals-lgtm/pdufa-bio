# Audit — 2026-07-23
*Live API + Search Console. Not investment advice.*

---

# 🔴🔴 THE REFRESH IS STILL DEAD — now 12 days, and the clock is about to catch it

Flagged 2026-07-19 and again 2026-07-21. **Unaddressed, and now materially worse.**

```
as_of      = 2026-07-23   ← advances to "today" every day
updated_at = 2026-07-11T15:23:27Z   ← ALL 384 rows. Frozen 12 days.
```

The `as_of` field marching forward while `updated_at` sits still is the tell: **something is stamping "today" onto a dataset it is not actually rebuilding.** `/coverage` still claims *"refreshes ~5×/day via cron."* Twelve days, one timestamp. The cron is dead and nothing is alerting.

### The staleness hasn't dropped a *decided* event yet — but it's one day from doing so
- **Otsuka centanafadine — PDUFA tomorrow, July 24 (ADHD, first-in-class)** — still **completely absent** from the calendar. It never made it in because it needed a refresh after July 11.
- The next listed PDUFA is **MNKD on July 26.** After tomorrow, a real FDA decision (centanafadine) will resolve on a catalyst the site never showed — the exact failure a "most current calendar" cannot have.
- The decided archive still ends at **CELC 2026-07-17.** Anything that resolves this week lands into a frozen dataset.

**This is now the only thing standing between the site and its own positioning.** Every other P0 is fixed. The pitch is "most up to date." The data is 12 days old and about to visibly miss a decision. Nothing else on the board matters until the pipe runs — ideally off the new Polygon feed (see prior audit's migration map).

**Minimum viable fix if the full cron can't be revived today:**
1. Manually add centanafadine (7/24) + any post-7/11 confirmations so tomorrow isn't a visible miss.
2. Flip the "Live" badge to read the real `max(updated_at)` — "Updated Jul 11" is honest; "Live" is not.
3. Ship the `test_data_freshness.py` guard from `AUDIT_2026-07-21.md`.

---

# 🟠 NEW — Search Console flags your Event schema as 94% invalid

GSC Overview → Recommendations, verbatim:
> **"Events: 94% of your items aren't eligible for rich results. Review and fix issues making your items invalid."**

The homepage now emits `Event` structured data (the P0 fix from 07-19 — good), **but Google is rejecting almost all of it.** Event rich results have required fields (`location` with a real `name`/`address`, valid `startDate` with timezone, `eventAttendanceMode`, `eventStatus`). The homepage Events use `VirtualLocation` — which for a *physical* FDA decision is semantically wrong and likely the rejection cause, or the `startDate`/location fields are incomplete.

**This matters because rich results are the one on-SERP advantage available to a site with no backlinks.** An eligible Event can show a date chip in search; an invalid one shows nothing. **Builder: open GSC → Enhancements → Events, read the exact error, fix the required fields.** Cheapest visibility win on the board now that the schema is at least present.

---

# 🟢 SEO — the sitemap fix is already working

Verified this session by inspecting individual URLs in Search Console:

- **`/ticker/MRNA` and `/ticker/VRTX` are now "URL is on Google" — indexed.** Both were unindexed a week ago. **The ticker hubs are getting picked up from the www sitemap.** The structural fix (delete non-www sitemap + resubmit www) is landing.
- Indexing report unchanged in shape: **10 indexed / 350 not**, of which **315 = "Discovered – currently not indexed"** (known, uncrawled — the crawl-budget problem). But the trend is now correct: sitemap-discovered hubs are converting to indexed.
- `/calendar` impressions **down 93%** (GSC insight) — consistent with the canonical/duplicate churn from the redirect work; should recover as `/calendar` settles as the self-canonical.
- Clicks ticked **42 → 44.** Marginal, expected — this is the slow build you flagged.

### Requested indexing this session (10 — all confirmed queued)
High-value, verified *not-indexed* before requesting (skipped MRNA + VRTX — already indexed, no quota wasted):
`/ticker/LLY` · `/ticker/PFE` · `/ticker/MRK` · `/ticker/GILD` · `/ticker/BMY` · `/calendar/2026/august` · `/calendar/2026/september` · `/calendar/2026/october` · `/calendar/2026/november` · `/adcomm`

Rationale: big-pharma ticker hubs (head-term "MRK FDA calendar" etc., near-zero competition) + the forward calendar-month pages (they *should* rank for "August 2026 PDUFA calendar" — a query with real volume and weak incumbents) + `/adcomm` (almost nobody optimizes for AdComm calendars).

---

# ✅ Still-holding fixes (re-confirmed)
P0-A fan-out gone · null drug names 0 · readout precision `month` · sitemap 520 www-only · homepage Event schema present (just needs validity fix) · redirect topology clean.

---

# The order
1. 🔴 **Revive the refresh (Polygon) — or manually backfill centanafadine before tomorrow.** Make "Live" honest. This is the whole ballgame for the positioning.
2. 🟠 **Fix Event schema validity** (GSC → Enhancements → Events). Rich-result eligibility is your only no-backlink SERP edge.
3. 🟢 **Let the 10 requests + sitemap land.** Ticker hubs are already converting — the mechanism works, it just needs volume + time. Don't over-request; the lever now is currency + links, not quota.

**One sentence:** the site is correct, well-structured, and finally getting indexed — but it's advertising "live" data that's twelve days stale and about to miss tomorrow's FDA decision, and that single gap now outweighs everything else combined.

---
*Facts and historical statistics only. Not investment advice.*

Sources: centanafadine PDUFA July 24 2026 — https://lifesciencedaily.news/fda-drug-approval-decisions-to-watch-july-and-august-2026/
