# Audit — 2026-07-27
*Live API + Search Console. Not investment advice.*

**Two fronts: (1) Lipfendra was added and the builder-prompt item #1 is done, but the full rebuild has NOT run — 326 rows still frozen at July 11. (2) SEO is turning the corner — primary pages are indexing — but the Event-schema invalidity is now quantified and it's large.**

---

# ✅ Lipfendra fix shipped (builder-prompt item #1)

The record is now present and correct:
```json
{ "ticker":"MRK", "name":"Lipfendra (enlicitide decanoate)",
  "date":"2026-07-16", "decision_date":"2026-07-16",
  "status":"Decided", "outcome":"Approved",
  "indication":"Hypercholesterolemia incl. HeFH (LDL-C lowering)",
  "url":"https://www.fda.gov/.../fda-approves-first-oral-pcsk9-inhibitor..." }
```
Correct ticker, decision date, outcome, indication, and FDA source link. The immediate miss is closed.

---

# 🔴 The full rebuild has NOT run — items #2/#3 still open

```
2026-07-27 : 59 rows   ← today (near-term + decided + the manual Lipfendra add)
2026-07-26 :  1
2026-07-25 :  1
2026-07-11 : 326 rows  ← STILL frozen (day 16) → mode → badge "Data through Jul 11"
```

The builder hand-added Lipfendra (correct) but the **automated full rebuild + backfill from the builder prompt hasn't happened.** 326 rows — the bulk of future readouts and later-2026 PDUFAs — are unchanged since July 11, and the badge still honestly reads "Data through Jul 11" with the amber dot. **The FDA reconciliation guard, the daily full rebuild, and the CI freshness check are the remaining asks.** Until they land, every future manual add is a patch, and the next Lipfendra-class miss is a matter of time. This stays the #1 item.

*(Minor, related: the `Data through {date}` badge is computed client-side only — the server-rendered HTML shows the fallback "2026 · free, no login". A non-JS crawler never sees the freshness date. Worth server-rendering it.)*

---

# 🟢 SEO — the primary pages are indexing. Real movement.

Inspected every primary page in Search Console this session. **The sitemap fix + prior requests are landing:**

| Page | Last audit | Now |
|---|---|---|
| `/calendar` | "Duplicate, Google chose different canonical" (not indexed) | ✅ **Indexed** |
| `/conferences` | "Unknown to Google, never crawled" | ✅ **Indexed** |
| `/decisions` | gaining | ✅ **Indexed**, clean, all enhancements |
| `/readouts` | indexed | ✅ Indexed |
| `/screener` | indexed | ✅ Indexed, clean |
| `/condition/cancer` | just requested | 🟢 GSC insight: "recently got **more** impressions than usual" |

**The `/calendar` canonical problem is resolved** — that was the headline SEO blocker for weeks. Requested a fresh recrawl on all five primary pages this session (they now carry the new decisions incl. Lipfendra).

---

# 🟠 Event schema invalidity — now quantified, and it's substantial

GSC's per-page Enhancement counts make the "94% of Events not eligible" concrete:

| Page | Events status |
|---|---|
| `/calendar` | **55 invalid** items |
| `/readouts` | **150 invalid** items |
| `/conferences` | **14 VALID** items (non-critical issues only) |

**The diagnosis is now precise:** the **conference** Event objects validate fine (real venues, real dates) — it's the **PDUFA and readout** Event objects that are invalid (~205 across two pages). That's consistent with the `startDate` being date-only + `VirtualLocation` for what Google wants modeled with a proper time. **This is the one unshipped item from the builder prompt's "minors" that has real SERP cost** — 205 event objects on your two highest-value index pages are ineligible for date-chip rich results. Fix: add time+TZ to `startDate` on the PDUFA/readout event emitter (leave conferences alone — they pass). Then recrawl.

---

# ✅ No regressions
Fan-out **0** · null drug names **0** · every Decided event has an outcome · sitemap 520 www-only · Lipfendra correct.

---

# The order
1. 🔴 **Run the automated full rebuild + backfill + FDA reconciliation guard** (builder prompt items #2/#3). 326 rows / day 16. This is still the whole game.
2. 🟠 **Fix the PDUFA/readout Event `startDate`** (add time+TZ) → clears 205 invalid items across `/calendar` + `/readouts`. Conferences already pass, so scope the fix to the PDUFA/readout emitter.
3. 🟡 Server-render the "Data through" badge; sort decided archive by `decision_date`.
4. 🟢 Indexing is working — keep letting it land; the recrawls requested today carry the new decisions.

**Indexing requested this session (5 primary pages, recrawl):** `/calendar`, `/decisions`, `/conferences`, `/readouts`, `/screener` — all confirmed queued.

**One sentence:** the flagship SEO blocker (calendar canonical) is finally resolved and the primary pages are indexing, but the site is still serving "Data through July 11" data behind them, and the one thing that turns this from "improving" into "current and defensible" is running the automated rebuild — everything else is now downstream of that.

---
*Facts and historical statistics only. Not investment advice.*
